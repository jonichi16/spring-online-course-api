
# Database and database credentials
spring.datasource.url = jdbc:mysql://localhost:3306/spring_online_course?useSSL=false
spring.datasource.username = root
spring.datasource.password =

# The SQL dialect defines what type of database is in use so that it can generate the appropriate statements.
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Allows to update the database schema based on java entities found in the project
spring.jpa.hibernate.ddl-auto = update

# To generate secret key, run the following command in gitbash: openssl rand -hex 32
jwt.secret = aa1ae4bf9c738f4b2bc2f268418921a061adce44da8ebf42ab5df238273e619d

#spring.main.allow-circular-references=true
